# ============================================================================
# DEPRECATED: This Dockerfile is kept for reference only.
# The active backend Dockerfile has been moved to repo root as:
#   backend.Dockerfile
#
# Railway and CI/CD now use the root-level Dockerfile with repo root context
# to properly access packages/shared/python.
#
# Build command: docker build -f backend.Dockerfile -t backend .
# ============================================================================

FROM python:3.11-slim
CMD ["echo", "Use backend.Dockerfile at repo root instead"]
